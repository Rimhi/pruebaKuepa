<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Welcome!{% endblock %}</title>
        {% block stylesheets %}
        <style>
            .clearfix{
                float:none;
                clear:both;
            }
            body{
                background:white;
            }
            .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
            .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
            .header{

            }
            .menu{
                margin:0px;
                padding:0px;
                list-style:none;
                width:100%;
                height:70px;
                line-height:50px;
            }
            .menu li{
                margin:0px;
                padding:0px;
                display:block;
                float:left;
                padding-left:15px;
                padding-right:15px;
                border-right:1px solid #ccc;
                cursor:pointer;

            }
            .menu a{
                display:block;
                text-decoration:none;
                color: #ccc;
                transition: all 300ms;
            }
            .menu li:hover{
                background-color: #F86433;
              
            }
            input[type="text"],
            input[type="password"],
            input[type="email"],
            input[type="number"],
            select{
                width:100%;
                padding:10px;
                font-size:15px;
                

            }
            input[type="submit"]{
                padding:15px;
                margin-top:10px;
                font-size:15px;
                background-color: #F86433;
                color:white;
                cursor:pointer;
                transition: all 300ms;

            }
            input[type="submit"]:hover{
                background-color: #F86433
            }
            label,h1{
                color: #F86433;
            }
            .cright{
                float:right !important;
            }
            table th{
                padding-left:30px;
                padding-right:30px;
                border-bottom: 1px solid  #F86433;
                text-align: center;
            }
            table td{
                padding-left:30px;
                padding-right:30px;
                border-bottom: 1px solid  #ccc;
                text-align: center;
            }
        </style>
            
        {% endblock %}
    </head>
    <body>
        {% block headers %}
            <div class="header">
                <img src="http://www.kuepa.com/COV2/assets/img/logo.png">
                <ul class="menu">
                    
                    {% if app.user %}
                        <li class="cright"><a href="{{path('logout')}}">Cerrar Session</a></li>
                        {% if app.user.role == "ROLE_ADMIN" %}
                            <li class="cright"><a href="{{path('users')}}">Usuarios</a></li>
                        {% endif %}
                        <li class="cright"><a href="#">{{app.user.name ~ ' ' ~ app.user.surname}}</a></li>

                    {% else %}
                        <li><a href="{{path('register')}}">Registro</a></li>
                        <li><a href="{{path('login')}}">Login</a></li>
                    {% endif %}   
                </ul>
            </div>
            <div class="clearfix"></div>
        {% endblock %}
        {% block body %}{% endblock %}
        {% block javascripts %}{% endblock %}
    </body>
</html>
